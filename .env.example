# Copy this file to .env.local (or .env) and populate with your Supabase project settings.
# Values are read at build time by Vite; restart the dev server after making changes.

VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_PROJECT_ID="your-project-ref"
DEFAULT_SUPABASE_ANON_KEY="your-anon-key"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"
